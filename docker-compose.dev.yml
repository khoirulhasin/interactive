version: '3'

services:
  app:
    container_name: backend-ais
    env_file: 
     - .env.development
    depends_on:
      - postgres
    command: npm run dev
  postgres:
    container_name: database-postgres-ais
    env_file: 
     - .env.development